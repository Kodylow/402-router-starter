
const middleware_fedimint = (xFedimintHeader: string): boolean => {
    return true;
}

const middleware_cashu = (xCashuHeader: string, exactAmount: number): boolean => {
    // Decode to a cashu token

    // Check the token is valid and for exact amount

    // Try to reissue the token against the mintUrl

    // If it fails, return false

    // If it succeeds, return true
    return true;
}

const middleware_l402 = (authorizationHeader: string): boolean => {
    return true;
}
